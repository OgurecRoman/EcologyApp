{
  "name": "server",
  "version": "1.0.0",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "postinstall": "npx prisma generate && npx prisma migrate deploy",
    "test": "echo \"Error: no test specified\" && exit 1",
    "db:deploy": "npx prisma migrate dev --name init && npx prisma deploy",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@prisma/adapter-pg": "^6.19.0",
    "@prisma/client": "^6.19.0",
    "ejs": "^3.1.10",
    "express": "^5.1.0",
    "pg": "^8.16.3",
    "prisma": "^6.19.0"
  }
}
