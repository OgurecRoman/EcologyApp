# 1. Базовый образ
FROM node:18-alpine

# 2. Рабочая директория внутри контейнера
WORKDIR /app

# 3. Копируем package.json для установки зависимостей
COPY package*.json ./

# 4. Устанавливаем зависимости (production-only если нужно)
RUN npm install

# 5. Копируем ВСЁ содержимое папки app внутрь контейнера
COPY . .

# 6. Указываем порт (если бот слушает вебхук или API)
EXPOSE 3000

# 7. Команда запуска
CMD ["node", "bot.js"]
